<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/movie_grey</item>
        <item name="colorPrimaryDark">@color/movie_dark_grey</item>
        <item name="android:windowBackground">@color/movie_dark_grey</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="titleTextStyle">@style/MyTitleTextStyle</item>
    </style>

    <style name="MyTitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/title_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">21sp</item>
    </style>

    <style name="DetailTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/movie_dark_grey</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/menu_text</item>
        <item name="colorControlActivated">@color/star_color</item>
    </style>

</resources>
